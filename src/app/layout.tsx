import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Blackfire Service - Stock Investment Analysis',
  description:
    'Comprehensive stock analysis platform with AI-powered insights, portfolio management, and real-time market data.',
  keywords: ['stocks', 'investment', 'portfolio', 'analysis', 'market data'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}
